# Plan.B 프론트엔드 수정 내역 (클론 이후)

이번 세션에서 수정한 항목을 영역별로 정리했습니다. 각 항목은 **증상 → 원인 → 수정(파일)** 순서입니다.

---

## 1. Android 지도 안 보임 / iOS Google 지도 통일

**증상:** iOS는 지도가 보이는데 Android는 회색으로 안 보임.

**원인:** MapView 스타일이 `StyleSheet.absoluteFillObject`(stretch)였는데, Android의 react-native-maps가 이 stretch 높이를 0으로 측정 → 지도 영역 높이 0. (iOS는 애플 지도라 이 버그를 안 탐)

**수정:**
- `src/components/planA/PlanAMapPreview.android.tsx`, `.native.tsx`, `.tsx` — MapView 스타일을 `flex: 1`(`styles.map`)로 변경.
- `src/screens/AddScheduleLocationScreen.native.tsx` — 검색바 오버레이 보존 위해 절대배치 유지하되 `bottom:0` 대신 **명시적 `height`** 부여.
- 모든 MapView `provider`를 `PROVIDER_GOOGLE`로 통일(iOS도 Google 지도). 미사용 `Platform` import 정리.

> 가이드: Android MapView에 `absoluteFillObject` 금지. 부모가 고정 높이면 `flex:1`, 오버레이 위에 있으면 `position:absolute + 명시적 height`.

---

## 2. 소셜 로그인(카카오) — 잘못된 "로그인 실패" 알림

**증상:** 카카오 로그인은 되는데 "알 수 없는 소셜 로그인 응답입니다" 알림이 뜸.

**원인:** 리다이렉트를 `LoginScreen`(WebBrowser)와 `OAuthRedirectScreen`(딥링크) 양쪽이 처리하는데, 후자가 oauth와 무관한 url을 받고도 무조건 실패 처리.

**수정:** `src/screens/OAuthRedirectScreen.tsx` — oauth 성공/실패 url이 아니면 조용히 무시. url로 결과를 못 얻으면 라우트 파라미터(`result`)로 화면 전환(스피너 무한 정지 방지).

---

## 3. 메인 화면 — "다음 여행" 목록/그룹화/간격

**증상:** 예정 일정을 여러 개 등록했는데 1개만 보임. 같은 여행이 장소별로 쪼개져 중복 표시. 섹션 간격 불균일.

**수정:** `src/screens/MainScreen.tsx`
- "다음 여행"을 `find`(1개) → `filter`(전부)로 변경해 예정 일정을 모두 나열.
- **새 일정 추가 버튼을 진행중 일정 ↔ 다음 여행 사이로 이동.**
- 같은 `이름+기간` 여행을 하나로 묶고(중복 제거) 장소 수가 가장 많은 항목을 대표로 표시.
- 섹션 간격 16px로 통일.
- "다음 여행" 카드의 위치 텍스트("장소 미정") 제거, **"N개 장소"만** 표시.

---

## 4. 메인 화면 로딩 속도 개선 (백엔드 연동)

**배경:** 백엔드가 `GET /api/trips` 응답에 `itineraryCount`/`placeCount` 추가.

**수정:**
- `api/schedules/server.ts` — `TripSummary`에 `itineraryCount`/`placeCount` 추가.
- `src/screens/MainScreen.tsx` — 홈 로드 시 모든 여행마다 `GET /api/trips/{id}` 호출하던 것 제거. 목록의 `placeCount`를 그대로 사용(`getPlaceCount`)하고, **진행중 여행 1건만** 상세 보강(현재 장소 표시용).

> 트레이드오프: 예정 여행 카드의 첫 장소명은 표시 안 함(개수만). 필요 시 목록 응답에 대표 장소명 추가 요청.

---

## 5. 진행중 일정 — 1일차/2일차 전환 시 안 보임

**증상:** 1↔2일차 왔다갔다 하면 선택한 날짜가 비어 보이고, 메인 갔다 오면 그제서야 보임.

**원인:** 재조회 중복 방지 키(`loadKey`)에 **선택 날짜가 빠져** 날짜를 바꿔도 "중복 호출"로 막힘.

**수정:** `src/utils/ongoing/loadTripDetail.ts` — `loadKey`에 `selectedDayIndex` 포함.

---

## 6. 빈시간 추천 — 60분 이상만 카드 노출

**요구:** 추천 가능 시간이 60분 이상인 구간만 추천 카드 표시, 그 외엔 카드 숨기고 공백은 유지.

**수정:** `src/components/recommendations/GapRecommendationCard.tsx`
- 서버 gaps를 `availableMinutes >= 60`만 남기도록 필터.
- 조건 미달이면 카드를 렌더하지 않음(`return null`). 공백/이동 구간은 상위에서 유지.

---

## 7. 빈시간 추천 — 장소 선택 시 수정 페이지로 이동 + 제안 시간 반영

**증상:** 빈시간 추천에서 장소 선택해도 "선택됨"만 되고 시간/이동수단 설정 화면으로 안 감.

**수정:**
- `src/components/recommendations/GapRecommendationCard.tsx` → `onSelectPlace` 연결.
- `src/components/ongoing/OngoingGapBetweenPlace.tsx` → `onSelectGapPlace` prop 전달.
- `src/screens/OngoingScheduleScreen.tsx`, `src/screens/UpcomingScheduleScreen.tsx` → `handleSelectGapPlace` 추가: 선택 시 `addTripLocation`으로 해당 날짜에 장소 추가 후 **PlanA 수정 페이지(`isEditMode: true`)로 이동.**
- 백엔드가 추가한 `suggestedVisitTime`/`suggestedEndTime`(이전 일정 종료~다음 일정 시작)을 `addTripLocation`의 `visitTime`/`endTime`으로 전달. (SOS/날씨 대안은 null이라 기존 로직 유지)

---

## 8. 대안찾기 / 날씨 대안 — 수정 페이지 이동 & 시간대 유지 & 알림 삭제

**수정:** `src/screens/RecommendationResultScreen.tsx`, `src/screens/PlanAScreen.tsx`, `src/screens/MainScreen.tsx`
- PLAN B 교체 / 날씨 대안 교체 후 **PlanA 수정 페이지(`isEditMode: true`)로 이동.**
- 날씨 대안 교체 시 알림의 `day`를 파라미터로 넘겨 **정확한 날짜로** 수정 페이지 열기.
- 교체 시간대 유지: 백엔드가 replace 응답에 원본 시간대 유지하도록 처리(프론트 변경 불필요).
- **대안이 없을 때:** "실내 대안을 찾지 못했어요. 일정을 조정하시겠어요?" 안내 → [일정 조정] 시 수정 페이지로. (좌표 없으면 `getPlaceDetail`로 좌표 보강 후 추천 시도)
- **알림 삭제 흐름:** 일정 조정으로 진입 시 `dismissNotificationId`를 PlanA에 전달 → **저장(시간/장소 수정) 완료 시점에** 해당 날씨 알림 삭제.

---

## 9. PlanA 수정 페이지 — 교체 후 옛 데이터 표시(캐시)

**증상:** 날씨 대안 교체 후 진행중 화면엔 새 장소가 보이는데, 수정 페이지엔 옛 장소가 보임.

**원인:** `usePlanAPlaces`에 `reloadKey`를 안 넘겨 항상 메모리 draft 캐시 사용 → 서버 최신본 미반영.

**수정:** `src/screens/PlanAScreen.tsx`, `src/hooks/usePlanAPlaces.ts` — `reloadKey: route.params.refreshPlanAAt` 전달 + 로드 effect 의존성에 `reloadKey` 추가. `refreshPlanAAt` 변경 시 draft 캐시 건너뛰고 서버 재조회.

---

## 10. 빈시간 추천 갭이 변경 후에도 옛것으로 유지

**증상:** 대안찾기로 일정 변경 후에도 빈시간 추천 갭이 옛 구간으로 몇 분간 남아있음.

**원인:** 갭 데이터 캐시(`tripGapsCache`)가 TTL 없는 영구 캐시.

**수정:** `src/components/recommendations/GapRecommendationCard.tsx` — 갭 캐시에 **20초 TTL** 추가. + `src/screens/RecommendationResultScreen.tsx` — 대안 교체 성공 시 `clearTripGapCache(tripId)` 즉시 호출(즉각 반영).

---

## 11. 장소 검색 — 지도 이동 / 카드 탭 / 마커

**증상:** 장소 검색 시 지도가 결과 위치로 안 움직임.

**원인:** 검색 API 응답에 좌표가 없음.

**수정:** `src/screens/AddScheduleLocationScreen.native.tsx`, `src/components/location/SearchResultCard.tsx`
- 검색 후 첫 결과로 지도 자동 이동, 카드 탭 시 해당 장소로 이동.
- 좌표가 없으면 `getPlaceDetail`로 좌표 보강 후 이동 + 포커스 마커 표시.

> 근본 개선: 검색 응답에 lat/lng 포함 시 보강 호출 불필요.

---

## 12. 장소 상세보기 — 빈 요약/원문 노출, 자동 재분석

**증상:** 상세보기 시 AI 요약이 안 된 빈/원문 상태가 먼저 보이고, 여러 번 시도해야 제대로 나옴.

**원인:** 백엔드가 `space != null`만으로 COMPLETE 판정(요약 미생성 fallback 포함). 또한 프론트의 "유의미한 리뷰" 판정이 원문이 들어오는 `googleReview` 필드까지 보고 오판.

**수정:** `src/hooks/location/usePlaceReview.ts`, `src/screens/AddScheduleLocationScreen.native.tsx`
- COMPLETE인데 AI 요약이 비었으면 **자동 1회 `reanalyze` + 재조회**로 진짜 요약을 채워 표시.
- "유의미한 리뷰" 판정을 **AI 요약 필드(`reviewSummary`/`aiSummary`)만** 보도록 수정(원문 필드 제외).

---

## 13. 프로필 — 선호 여행 스타일(preferredMoods)

**배경:** `GET /api/users/me`에 `preferredMoods` 추가.

**수정:** `api/users/me.ts`(타입), `src/screens/ProfileScreen.tsx` — 기존 `preferenceSummary` 대신 `me.preferredMoods` 사용. mood 코드→한글 라벨 매핑, 빈 배열 시 "아직 여행 이력이 없어요" 안내, 1~5개 가변 칩 표시.

---

## 14. 인증/토큰 — 403 갱신, 무한 루프 방지, 로그인 이동

**증상:** 토큰 만료 시 잦은 튕김 / "다시 조회하기" 무한 반복 / WAF 차단.

**수정:** `api/client.ts`, `src/utils/authEvents.ts`(신규), `App.tsx`
- 만료 토큰에 **401뿐 아니라 403도** 토큰 갱신 트리거(백엔드가 만료 시 403 반환).
- refresh가 **네트워크 일시오류가 아닌 진짜 무효(유효하지 않은 토큰 등)**일 때만 토큰 정리(로그아웃). 게이트웨이/네트워크 블립으로는 로그아웃 안 함.
- **refresh 실패 시 30초 쿨다운** — 추가 refresh를 막아 폭주/WAF 차단 루프 방지.
- 토큰 정리 시 `emitAuthExpired` → `App`이 **로그인 화면으로 자동 이동.**

---

## 15. 날씨 알림 카드 — 좌우 스와이프 전환 애니메이션

**수정:** `src/components/notifications/WeatherNotificationCard.tsx`, `src/screens/MainScreen.tsx` — `PanResponder`로 좌우 드래그 시 슬라이드 애니메이션으로 알림 전환(점 탭도 동일 애니메이션). 카드 `key` 고정(리마운트 방지).

---

## 16. 온보딩 2페이지 — 터치가 일부 영역만 동작

**증상:** 두 번째 온보딩에서 화면 아래쪽만 터치가 먹힘.

**원인:** 터치 레이어가 `flex:1`이라 미리보기와 공간을 반씩 나눠 아래 절반만 차지.

**수정:** `src/screens/OnboardingSecondScreen.tsx` — 루트를 `TouchableOpacity`로 감싸 화면 전체에서 다음으로 이동.

---

## 백엔드에 확인/요청할 사항

1. **`/api/auth/refresh` WAF/ELB 차단** — dev에서 refresh가 ELB 403(HTML)로 막힘. WAF 규칙/리스너 점검 필요.
2. **단일 세션 정책** — 로그인마다 기존 refresh token 삭제(`deleteByUser`) → 재로그인/멀티기기 시 잦은 만료. 멀티 세션 허용 검토.
3. **장소 상세 COMPLETE 판정** — `space != null`만으로 COMPLETE → 요약 빈 상태로 완료 처리됨. `reviewData` 생성 여부까지 확인하도록 수정 권장.
4. **빈시간 추천 스트림** — `/gaps/recommend/stream` 이 `done` 전 HTTP/2 INTERNAL_ERROR로 리셋, `/gaps` 500/반복 호출 시 서버 다운. 스트림 안정화·타임아웃·리소스 점검.
5. **dev 서버 간헐 502/500/네트워크 단절** — 안정성 점검.
6. **검색/알림 응답에 좌표(lat/lng) 포함** — 프론트 보강 호출 제거 가능.
7. **dev 환경 구글 OAuth registration** — `/oauth2/authorization/google` 리다이렉트 안 됨(client-id/secret 확인).

---

## 참고 — 무관한 로그(무시 가능)
- `expo-notifications: Custom sound 'default' not found` / `Default FirebaseApp is not initialized` — 푸시(FCM) 설정 관련. 지도/로그인/일정과 무관.
